#!/bin/bash
# live-build/config/bootstrap - Bootstrap settings for T430 OS
# NOTE: This file is read by live-build, NOT executed directly
# DO NOT put lb config commands here

# Bootstrap stage settings (automatically read by live-build)
# These override defaults but don't replace lb config command-line options

echo "Bootstrap configuration loaded for T430 OS"
echo "Base: Debian Bookworm (amd64)"
echo "Optimized for: Intel Ivy Bridge (T430)"
