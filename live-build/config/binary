#!/bin/bash
# live-build/config/binary - Binary image settings for T430 OS
# NOTE: This file is read by live-build, NOT executed directly
# DO NOT put lb config commands here

# Binary stage settings (automatically read by live-build)
# ISO Hybrid format with optimized boot parameters for T430

echo "Binary configuration loaded for T430 OS"
echo "Output: ISO Hybrid (USB bootable)"
echo "Boot options: elevator=none iommu=soft zswap.enabled=1"
echo "Optimized for SSD and Ivy Bridge GPU"
