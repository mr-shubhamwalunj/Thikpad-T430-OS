#!/bin/bash
# live-build/config/chroot - Chroot environment settings for T430 OS
# NOTE: This file is read by live-build, NOT executed directly
# DO NOT put lb config commands here

# Chroot stage settings (automatically read by live-build)
echo "Chroot configuration loaded for T430 OS"
